# Backend API Configuration
# This should point to your Spring Boot backend, NOT the React dev server

# Environment Configuration for Baby Cotton Club Frontend

# Backend URL Configuration
REACT_APP_BACKEND_URL=http://localhost:8080

# How it works:
# - All REST API calls go to backend at http://localhost:8080
# - Frontend dev server runs on http://localhost:3000
# - No need for proxy configuration in package.json
